# 📖 Chapter Summary - Class & Data

## 핵심 내용

- 클래스는 관련된 데이터를 하나로 묶기 위한 설계도이다.
- 객체는 클래스를 통해 생성된 실제 데이터이다.
- 객체는 서로 독립적인 값을 가진다.
- 객체 배열을 사용하면 여러 객체를 효율적으로 관리할 수 있다.
- 관련 데이터를 객체로 으면 코드의 가독성과 유지보수성이 향상된다.